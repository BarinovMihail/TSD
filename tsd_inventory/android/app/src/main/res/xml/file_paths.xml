<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- APK скачивается во временную директорию (getTemporaryDirectory → cache). -->
    <cache-path name="apk_cache" path="." />
    <!-- На случай записи во внешний кэш. -->
    <external-cache-path name="apk_ext_cache" path="." />
</paths>
